<?php

    $hostname = "localhost";
    $user = "root";
    $password = "";
    $dbname = "jay_acme";
    $port = 3306;

    $conn = new mysqli($hostname, $user, $password, $dbname, $port);

